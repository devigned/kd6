[project]
name = "crewai-kd6-example"
version = "0.1.0"
description = "CrewAI multi-agent example using KD6 as the memory backend"
requires-python = ">=3.10,<3.13"
dependencies = [
    "crewai[tools]>=1.14.0",
    "httpx>=0.27.0",
]

[project.scripts]
run-crew = "src.main:main"
smoke-test = "src.smoke_test:main"
